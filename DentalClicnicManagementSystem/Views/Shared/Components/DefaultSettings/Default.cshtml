@* @model CMS.Models.DefaultSettings

@{
    var displayType = ViewData["DisplayType"] as string ?? "full";
}

@if (displayType == "full")
{
    <div class="card border-0 bg-light">
        <div class="card-body py-2">
            <div class="row g-3 align-items-center">
                <div class="col-auto">
                    <i class="fas fa-cog text-muted"></i>
                </div>
                <div class="col">
                    <small class="text-muted d-block mb-1">Current Defaults</small>
                    <div class="d-flex flex-wrap gap-3">
                        <span class="badge bg-soft-primary text-primary">
                            <i class="fas fa-money-bill-wave me-1"></i>@Model.DefaultCurrency
                        </span>
                        <span class="badge bg-soft-success text-success">
                            <i class="ti ti-credit-card me-1"></i>@Model.DefaultPaymentMethod
                        </span>
                        <span class="badge bg-soft-info text-info">
                            <i class="ti ti-clock me-1"></i>@Model.WorkingHours
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
else if (displayType == "compact")
{
    <div class="d-flex gap-2">
        <span class="badge bg-soft-primary text-primary fs-12">@Model.DefaultCurrency</span>
        <span class="badge bg-soft-success text-success fs-12">@Model.DefaultPaymentMethod</span>
    </div>
}
else if (displayType == "inline")
{
    <span class="text-muted fs-13">
        Currency: <strong>@Model.DefaultCurrency</strong> |
        Payment: <strong>@Model.DefaultPaymentMethod</strong> |
        Hours: <strong>@Model.WorkingHours</strong>
    </span>
} *@


@model CMS.Models.DefaultSettings

@{
    var displayType = ViewData["DisplayType"] as string ?? "full";
}

@if (displayType == "full")
{
    <div class="card border-0 bg-light">
        <div class="card-body py-2">
            <div class="row g-3 align-items-center">
                <div class="col-auto">
                    <i class="fas fa-cog text-muted"></i>
                </div>
                <div class="col">
                    <small class="text-muted d-block mb-1">Paramètres par défaut actuels</small>
                    <div class="d-flex flex-wrap gap-3">
                        <span class="badge bg-soft-primary text-primary">
                            <i class="fas fa-money-bill-wave me-1"></i>@Model.DefaultCurrency
                        </span>
                        <span class="badge bg-soft-success text-success">
                            <i class="ti ti-credit-card me-1"></i>@Model.DefaultPaymentMethod
                        </span>
                        <span class="badge bg-soft-info text-info">
                            <i class="ti ti-clock me-1"></i>@Model.WorkingHours
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
else if (displayType == "compact")
{
    <div class="d-flex gap-2">
        <span class="badge bg-soft-primary text-primary fs-12">@Model.DefaultCurrency</span>
        <span class="badge bg-soft-success text-success fs-12">@Model.DefaultPaymentMethod</span>
    </div>
}
else if (displayType == "inline")
{
    <span class="text-muted fs-13">
        Devise : <strong>@Model.DefaultCurrency</strong> |
        Paiement : <strong>@Model.DefaultPaymentMethod</strong> |
        Heures : <strong>@Model.WorkingHours</strong>
    </span>
}
